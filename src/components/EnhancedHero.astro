---
import BlobShape1 from './icons/BlobShape1.astro';
import BlobShape2 from './icons/BlobShape2.astro';
import PhoneIcon from './icons/PhoneIcon.astro';
import ClockIcon from './icons/ClockIcon.astro';
---

<div class="hero-section py-12 md:py-16 lg:py-20 relative">
    <!-- Background shape decorations (svg) - FIXED SIZING -->
    <div class="absolute inset-0 overflow-hidden pointer-events-none">
        <BlobShape1 class="absolute -top-20 -right-20 text-primary opacity-10" />
        <BlobShape2 class="absolute -bottom-24 -left-24 text-accent opacity-10" />
    </div>

    <div class="container mx-auto px-4 relative z-10">
        <div class="max-w-4xl mx-auto text-center">
            <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold mb-6 leading-tight tracking-tight animate-fade-in">Break Free From Credit Card Debt <span class="text-primary">Save Up To 50%</span></h1>
            
            <p class="text-xl md:text-2xl text-text-muted mb-8 animate-fade-in-delay-1">Reduce Your Debt By Thousands | Become Debt-Free In <span class="text-primary font-semibold">12-36 Months</span></p>
            
            <div class="relative mb-12 max-w-3xl mx-auto">
                <!-- Pulsing background for emphasis -->
                <div class="absolute -inset-1 bg-gradient-to-r from-primary/20 via-primary/30 to-primary/20 rounded-lg blur-md"></div>
                <p class="relative bg-background-form dark:bg-slate-800 text-lg md:text-xl text-text-muted p-6 rounded-lg shadow-md mb-8 animate-fade-in-delay-2">
                    Are you struggling with <span class="font-bold">$15,000+ in credit card debt</span>? Our professional debt settlement program can help you reduce what you owe and become debt-free faster than making minimum payments.
                </p>
            </div>
            
            <div class="flex flex-col sm:flex-row justify-center gap-6 mb-6 animate-fade-in-delay-3">
                <a href="#consultation" class="btn btn-lg group relative overflow-hidden">
                    <span class="relative z-10">Get Your Free Consultation Now</span>
                    <div class="absolute inset-0 bg-gradient-to-r from-primary via-secondary to-primary bg-[length:200%_100%] animate-gradient"></div>
                </a>
                <div class="flex items-center justify-center gap-2 text-text-muted">
                    <PhoneIcon />
                    <span class="text-lg font-medium">(800) 555-1234</span>
                </div>
            </div>
            
            <p class="text-sm text-text-muted animate-fade-in-delay-3">
                <span class="inline-flex items-center gap-1">
                    <ClockIcon width="16" height="16" />
                    Available 9:00am - 8:00pm EST, 7 days a week
                </span>
            </p>
        </div>
    </div>
</div>

<style>
    /* Adding gradient animation for the CTA button */
    @keyframes gradient {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
    }
    
    .animate-gradient {
        animation: gradient 3s ease infinite;
        opacity: 0.9;
    }
    
    .group:hover .animate-gradient {
        opacity: 1;
    }
    
    /* Shadow effect on hover for buttons */
    .btn-lg {
        position: relative;
        transition: transform 0.2s ease, box-shadow 0.2s ease;
    }
    
    .btn-lg:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 15px -3px rgba(var(--color-primary-rgb), 0.4);
    }
    
    .btn-lg:active {
        transform: translateY(0);
    }
    
    @media (prefers-reduced-motion: reduce) {
        .animate-gradient {
            animation: none;
        }
        
        .btn-lg:hover {
            transform: none;
        }
    }
</style>