# API Keys (Required to enable respective provider)
ANTHROPIC_API_KEY="your_anthropic_api_key_here"       # Required: Format: sk-ant-api03-...
PERPLEXITY_API_KEY="your_perplexity_api_key_here"     # Optional: Format: pplx-...
OPENAI_API_KEY="your_openai_api_key_here"             # Optional, for OpenAI/OpenRouter models. Format: sk-proj-...
GOOGLE_API_KEY="your_google_api_key_here"             # Optional, for Google Gemini models.
MISTRAL_API_KEY="your_mistral_key_here"               # Optional, for Mistral AI models.
XAI_API_KEY="YOUR_XAI_KEY_HERE"                       # Optional, for xAI AI models.
AZURE_OPENAI_API_KEY="your_azure_key_here"            # Optional, for Azure OpenAI models (requires endpoint in .taskmasterconfig).
OLLAMA_API_KEY="your_ollama_api_key_here"             # Optional: For remote Ollama servers that require authentication.

# Supabase Configuration (Required for debt relief form backend)
SUPABASE_URL="your_supabase_project_url"              # Required: Format: https://your-project.supabase.co
SUPABASE_ANON_KEY="your_supabase_anon_key"            # Required: Public anon key from Supabase dashboard
SUPABASE_SERVICE_ROLE_KEY="your_supabase_service_role_key"  # Required: Service role key for server-side operations

# Email Configuration (Required for lead notifications)
RESEND_API_KEY="your_resend_api_key_here"             # Required: Format: re_...
RESEND_DOMAIN="yourdomain.com"                        # Required: Your verified domain for sending emails
RESEND_FROM_EMAIL="noreply@yourdomain.com"            # Required: From email address (must use verified domain)
RESEND_FROM_NAME="Your Company Name"                  # Optional: Display name for sent emails

# Internal Email Configuration (Required for lead notifications)
INTERNAL_NOTIFICATION_EMAIL="leads@yourdomain.com"    # Required: Where to send new lead notifications
ADMIN_NOTIFICATION_EMAIL="admin@yourdomain.com"       # Required: Admin email for system alerts

# Email Monitoring Configuration (Optional)
EMAIL_USAGE_ALERT_THRESHOLD=75                        # Optional: Alert threshold percentage (default: 75)
EMAIL_USAGE_CRITICAL_THRESHOLD=90                     # Optional: Critical alert percentage (default: 90)